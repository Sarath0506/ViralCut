# Environment & secrets (never commit)
.env
.env.local
.env.*.local
*.pem
credentials.json

# Vite SPAs — local API URL (commit .env.example only)
apps/web-brand-spa/.env
apps/web-agency-spa/.env

# Node / monorepo build artifacts
node_modules/
dist/
.turbo/
*.tsbuildinfo
packages/ui-tokens/dist/

# Next.js
.next/
apps/web-brand-spa/.env.local
apps/web-agency-spa/.env.local

# Prisma client is generated; migrations ARE committed for production deploy
# (do not ignore services/api/prisma/migrations/)

# Flutter / Dart
.dart_tool/
.packages
build/
**/.flutter-plugins
**/.flutter-plugins-dependencies

# IDE & OS
.idea/
.vscode/
*.iml
.DS_Store
Thumbs.db

# API local uploads (reference images/videos); DB stores URLs only
services/api/uploads/

# Logs, temp, coverage
*.log
tmp/
temp/
coverage/

# Cursor IDE (local / team-specific)
.cursor/
.stitch/
# Stitch design exports (optional — keep locally only)
design/stitch/