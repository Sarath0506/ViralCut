/** Placeholder until orval generates clients from OpenAPI. */
export type { ApiError, UserRole, SubmissionStatus } from "@viralcut/shared-types";

export const API_BASE_URL =
  process.env.NEXT_PUBLIC_API_URL ?? "http://localhost:3001";
