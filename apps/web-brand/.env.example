# Local dev — copy to .env.local (gitignored)
NEXT_PUBLIC_API_URL=http://localhost:3001

# API BRAND_WEB_URL must match this app's public origin (password reset emails).
# Local: http://localhost:3000  |  Prod: your Railway web-brand URL

# Production (Railway @viralcut/web-brand Variables tab only):
# NEXT_PUBLIC_API_URL=https://your-api.up.railway.app
