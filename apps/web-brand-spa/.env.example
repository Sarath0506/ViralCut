# Local dev — copy to .env (gitignored)
VITE_API_URL=http://localhost:3001

# API BRAND_WEB_URL must match this app's public origin (password reset emails).
# Local: http://localhost:3000  |  Prod: your deployed web-brand-spa URL

# Production:
# VITE_API_URL=https://your-api.up.railway.app
