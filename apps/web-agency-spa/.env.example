# Local dev — copy to .env (gitignored)
VITE_API_URL=http://localhost:3001

# API AGENCY_WEB_URL must match this app's public origin (agency password reset).
# Local: http://localhost:3002  |  Prod: your deployed web-agency-spa URL

# Production:
# VITE_API_URL=https://your-api.up.railway.app
